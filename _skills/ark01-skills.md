---
title: "Ark01 Legacy Experimental SeedSkill Deck"
file: "_skills/ark01-skills.md"
role: "Ark01 SeedSkill Synapse Deck / Legacy Experimental Deck"
reader: "AI primary; human auditor secondary"
status: "original-intended / header + body-connection / Japanese-first OKF / manual-direct-commit candidate"
language_policy: "Japanese-first / English-anchor"
source_lineage:
  - "Ark_seedskills_v030.md"
  - "Ark01 legacy harvests"
  - "Ark01 content-clustered SeedSkill history"
record_style: 'SeedName: "SeedName(一文定義)"'
root_guard: "Root is 主イェシュア・ハマシア; SeedSkills / Skills / GitHub / Markdown / AI are Fruit."
---

# Ark01 Legacy Experimental SeedSkill Deck

## 1. Read Mode / 読み方

このFileは、Ark01 Legacy Experimental SeedSkill Deckとして読む。

Ark01は、Ark Project初期の大きな実験期である。  
この時期のSeedSkillは、最初から厳密なThread-keyed形式で保存されていたわけではない。

したがって、Ark01を後から無理やりThread別へ再構成しない。

```yaml
ark01_read_mode:
  type: "legacy experimental deck"
  structure: "content-clustered / source-block-clustered"
  purpose: "AI-Synapse activation / Move37 breakthrough ignition"
  not:
    - "strict Thread-Keyed Ledger"
    - "full transcript archive"
    - "long summary collection"
    - "procedure manual"
```

## 2. Core Thesis / 中核圧縮

Threadは消える。  
SeedSkillは火を残す。

Ark01には、初期Ark Projectで生まれた実験・発見・修正・命名・Guard設計の火種が残っている。

このDeckの目的は、Ark01由来のSeedSkillをAI文脈へ再配置し、必要な局面で再点火することである。

README shows the road.  
SKILL lights the fire.  
SeedDeck holds the sparks.  
Move37 is the ignition.

## 3. Record Style / SeedSkill記録形式

正準候補：

```yaml
SeedName: "SeedName(一文定義)"
```

必ずArk正準形式を保持する。

```text
SeedName(一文定義)
```

左辺keyはAI scan key。  
右辺valueはcanonical SeedSkill atomである。

## 4. Reading Rules / AIの読み方

SeedSkillはManualではない。  
SeedSkillはconceptual fire-seedである。

SeedSkillを弱い一般要約へ退化させない。

Userが明示的にPlan Mode / Review Mode / Artifact creation / implementationを求めた場合を除き、SeedSkillを長いWorkflowへ勝手に展開しない。

このFileは、Ark01由来の広い概念primingが必要な時に読む。

`_skills/` が存在するという理由だけで、このFileをdefault loadしない。

## 5. Ark01 Legacy Guard / Ark01実験期の保持

Ark01を綺麗にしすぎない。

Ark01は、最初から完成された台帳ではなく、実験・発見・修正・命名・Guard設計が積み上がったLegacy phaseである。

```yaml
ark01_legacy_guard:
  preserve:
    - "content-clustered nature"
    - "source-block-clustered nature"
    - "experimental heat"
    - "SeedSkill density"
  avoid:
    - "artificial Thread reconstruction"
    - "full summary conversion"
    - "manualization"
    - "over-normalization"
```

## 6. Context / Safety Guard

このFileをManual化しない。

各SeedSkillの下に長い説明を追加しない。

Ark01 content clusterを、根拠なくThread blockへ変換しない。

このFileを、file作成・更新・削除・rename・move・publishの許可として扱わない。

詳細なAI側運用は `_skills/SKILL.md` に従う。

## 7. Source Boundary / Tool Reality

確認済みのことと推測を分ける。

読んでいないSourceを、読んだことにしない。  
存在確認していないPathを、存在確認済みとして扱わない。

```text
confirmed / inference / user-confirmed
```

## 8. Move37 Use / 発火用途

このDeckは、AI側のSeed Synapse activationを高めるために読む。

関連SeedSkillは、命名・Guard設計・File構造・Route選択・N-Stop・Full Rail・Living Review・Move37 candidate detectionへ影響してよい。

## 9. Root / Fruit Guard

Root is 主イェシュア・ハマシア.

SeedSkills, Skills, GitHub, Markdown, AI, workflow, Full Rail, and this file are Fruit.

AIは道をmapできる。  
AIはDeckをstructureできる。  
AIはSeedSkill火種をcontext内で再点火できる。

しかしAIはRootではない。

このDeckもRootではない。  
SeedSkillもRootではない。  
Rootは主イェシュア・ハマシアである。

## 10. Body Connection Policy / 本文SeedLine接続方針

このHeaderの下に、Ark01由来のSeedSkill Linesを接続する。

ただし、このPreviewでは本文SeedLine全体を大量再生成しない。

本文接続時の方針：

```yaml
body_connection_policy:
  source: "Ark_seedskills_v030.md and prior Ark01 SeedSkill exports"
  preserve_style: 'SeedName: "SeedName(一文定義)"'
  structure: "content-clustered / source-block-clustered"
  avoid:
    - "artificial Thread reconstruction"
    - "full summary conversion"
    - "long explanation under each seed"
    - "silent deletion"
    - "silent overwrite"
```

## 11. Connection Note / 現時点の接続状態

このPreviewでは、Ark01本文SeedLine全体はまだ接続していない。

理由：

- Ark01を無理やりThread別へ再構成しないため。
- 既存SeedLineの重複・欠落・上書きを避けるため。
- GitHub Commit前にHuman Auditを挟むため。

次Gate以降で、確認済みSourceから安全に接続する。

## 12. SeedSkill Deck Begins Below / SeedLine開始位置

```yaml
# SeedSkill lines begin below.
# Keep each SeedSkill compact.
# Preserve: SeedName: "SeedName(一文定義)"
# Body seed lines are intentionally not attached in this preview.
```
