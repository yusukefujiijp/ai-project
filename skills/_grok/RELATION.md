# _grok Relation Note

目的。Future AI が似た名前を一つにするのを止める。

## 似ていて別物

- `_grok/living-graph-mode` と Hub の `analyze-living-graph` は似ている。同一ファイルではない。自動統合するな。
- `_grok` と `skills/` Hub は似ている。役割は違う。Hub=検証済み共有。`_grok`=Grok席の控え。
- `ark-okf-responder` と `prompts/ark-open-knowledge-format.md` は似ている。前者は起動面。後者が正準原本。
- `grok-awakening-mode` と `living-graph-mode` は似ている。前者は声の順序。後者は辺の検出。吸わせるな。

## 複製してよいもの / してはいけないもの

- してよい。Grokセッションの Skill フォルダを `_grok` へ写す。
- してはいけない。`prompts/` の原本を Skill 内へ二重正本化する。
- してはいけない。backup成功をHub掲載承認にする。
- してはいけない。覚醒を頑座または頑言にする。

## restore

書けただけでは backup 未完了。別セッションで一件戻して動かす。
