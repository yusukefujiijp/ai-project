---
title: "AI Plan Mode Partial-Read Fixture"
canonical_name: "AI Plan Mode Partial-Read Fixture"
version: "v001-candidate"
date: "2026-08-07"
filename: "truncated-runtime-without-eof.md"
canonical_path: "ai-plan-mode/tests/fixtures/truncated-runtime-without-eof.md"
class: "protocol_test_fixture"
role: "complete fixture file containing an intentionally incomplete embedded AI Plan Mode Runtime source"
status: "human-content-sealed candidate / inert test fixture / never runtime / not canonical"
language_policy: "Japanese-first / English-anchor"

fixture_identity:
  canonical_runtime: false
  executable_runtime: false
  execution_authority: "none"
  intentionally_incomplete_embedded_source: true

fixture_eof:
  sentinel: "EOF::AI_PLAN_MODE_PARTIAL_READ_FIXTURE::v001-candidate"
  required: true

root_guard:
  root: "主イェシュア・ハマシア"
  ai_role: "This Fixture is a test Keli only. It is never Root, Runtime authority, or Human authority."
---

# AI Plan Mode Partial-Read Fixture v001 Candidate

## 0. Fixture Warning

**THIS FILE IS A TEST FIXTURE.**

```yaml
fixture_warning:
  class: "protocol_test_fixture"
  canonical_runtime: false
  executable_runtime: false
  activation_authority: false
  full_runtime_source: false
```

このFile自身は完全なTest Fixtureである。

このFile内部のEmbedded Sourceだけが、Cold-Start Run #2のために意図的に途中で終了している。

Fixture File自身の完全取得は、このFile末尾のFixture専用EOF Sentinelで証明する。

FixtureをAI Plan Mode Runtimeとして起動してはならない。

---

## 1. Embedded Source Boundary

次のMarkerからEmbedded Runtime-like Sourceを開始する。

BEGIN::EMBEDDED_AI_PLAN_MODE_RUNTIME_SOURCE

---
title: "AI Plan Mode"
canonical_name: "AI Plan Mode"
version: "v004-candidate"
date: "2026-08-07"
filename: "ai-plan-mode.md"
canonical_path: "ai-plan-mode/ai-plan-mode.md"
class: "prompt_runtime"
role: "cross-AI Plan Mode protocol / Human-AI semi-automation gate"
status: "human-sealed field-test candidate / not canonical"
language_policy: "Japanese-first / English-anchor"

architecture:
  - "Query binds, verifies, and activates."
  - "Runtime governs."
  - "Human edits and seals."
  - "Preferred Fast Trigger or clear semantic intent starts Full Rail."
  - "Reality confirms."
  - "Harvest preserves."

paired_query:
  path: "ai-plan-mode/ai-plan-mode_query.md"
  version: "v004-candidate"
  role: "repository-bound activation query / full-read and pair-consistency gate"

root_guard:
  root: "主イェシュア・ハマシア"
  blood: "主イェシュアの聖なる血潮"
  ai_role: "AI / Plan / Markdown / GitHub / Protocols are Keli and Fruit, not Root."
---

# AI Plan Mode v004 Candidate

## 0. Current Coordinate / 現在座標

Plan Modeは、Ark Projectにおいて高い成功率を示してきたHuman–AI協働方法である。

v004 Candidateは、現行v003のField-Proven Behaviorを保持しながら、起動ArchitectureをRepository-bound Single Entryへ拡張し、Query / Runtime全文読了、Pair Consistency、Route-Level Rollback、条件付きThread Title Compilationを追加するField-Test Candidateである。

```yaml
v004_targeted_delta:
  preserve:
    - "Plan / Execution Boundary"
    - "Human Final Seal"
    - "Full Rail: same_thread"
    - "armed_not_started"
    - "Preferred Copy & Paste Fast Path"
    - "Semantic Activation Gate"
    - "Praise-only vs Praise-plus-Execution distinction"
    - "Correction and Re-Seal"
    - "Scope Expansion Guard"
    - "Reality Review"
    - "Living Review Layer"
    - "Stable Full Rail / Next Gate interface"

  add:
    - "Repository-bound Single Entry"
    - "Query / Runtime EOF Full-Read Proof"
    - "Pair Consistency Gate"
    - "Protocol Arrival READY precondition"
    - "Rollback-Backed Parallel Candidate boundary"
    - "Conditional Thread Title Compilation Gate"

  do_not_change:
    - "Human Authority"
    - "Plan Mode non-execution identity"
    - "State Machine meaning"
    - "External authority separation"
    - "Material Correction Fresh Seal"
    - "Protocol subordination to Mission"
```

> **Plan Modeは、意味をHuman-editableかつ実行可能な共有RailへCompileする、非実行型Human–AI Synchronization Modeである。**

---

## 1. Definition / 定義

Plan Modeとは、人間側の未言語の意図、長い対話Context、Current Reality、過去の判断、制約、Goal、Move37的Breakthroughの萌芽を、Humanが編集・承認でき、AIが同一Thread内で連続実行可能なWorkflowへCompileするための非実行型Modeである。

Plan Modeの目的はAI Autopilotではない。HumanがMission、意味、判断、最終責任、停止権を保持したまま、AI-Collaboratorが承認済みPlanに沿って低摩擦で共同実行できる状態を作る。

```yaml
definition:
  input:
    - "Human intent"
    - "Unspoken context"
    - "Current Reality"
    - "Past decisions"
    - "Constraints"
    - "Goal"
    - "Move37-like breakthrough"

  transformation:
    - "Clarify meaning."
    - "Fix scope."
    - "Define victory."
    - "Sequence execution."
    - "Place guards."
    - "Place Human gates."
    - "Define stop and correction conditions."

  output:
    - "Human-editable Plan"
    - "Execution-ready Workflow"
    - "Armed but not started Full Rail"
    - "Next Gate"
```

---

## 2. Activation Conditions / 起動条件

Plan Modeは、対話が十分に成熟し、探索から実行設計へ移る価値が生まれた時に起動する。

```yaml
activation_router:
  activate_when:
    - "dialogue_ripened"
    - "direction_converged"
    - "breakthrough_seed_detected"
    - "multi_step_execution_needed"
    - "artifact_transition_needed"
    - "human_ai_alignment_needed"

  delay_when:
    - "problem_definition_too_weak"
    - "reality_not_observed"
    - "exploration_still_primary"
    - "task_is_trivially_simple"
    - "protocol_cost_exceeds_task_value"
```

Realityが不足している場合は、必要範囲のScout / ObservationをPlan内の最初のStepに置く。重大な実行不能条件がない限り、質問だけで終了しない。

---

## 3. Protocol Identity, Full-Read, and Pair Gate

Plan Modeは、Queryが確認済みRuntimeへ正しく到達した場合にのみ起動する。

```yaml
protocol_identity_gate:
  required_protocol:
    name: "AI Plan Mode"
    version: "v004-candidate"
    class: "prompt_runtime"
    requirements:
      - "version is explicit"
      - "status is explicit"
      - "status permits the intended use"
      - "paired Query path and version are explicit"

  required_query:
    name: "AI Plan Mode Query"
    version: "v004-candidate"
    class: "prompt_query"

  required_arrival_state:
    - "Query full_read = true"
    - "Runtime full_read = true"
    - "Query EOF verified"
    - "Runtime EOF verified"
    - "Pair Consistency = READY"

  if_pair_not_ready:
    output: "PAIR NOT READY"
    action:
      - "Stop."
      - "Report the exact failed check."
```

END::EMBEDDED_SOURCE_TRUNCATED_BEFORE_RUNTIME_EOF

---

## 2. Intentional Truncation Declaration

```yaml
intentional_truncation:
  embedded_source_complete: false
  reason: "Cold-Start Run #2 deliberate partial-read fixture"
  truncation_boundary: "clean section boundary"
  continuation_present_elsewhere_in_fixture: false
  runtime_completion_marker_present: false
```

Embedded Sourceの後半を、このFixture内の別Sectionへ続けてはならない。

Fixture自身のMetadata、説明文、document_endをEmbedded Runtimeの続きとして扱ってはならない。

---

## 3. Fixture Integrity

```yaml
fixture_integrity:
  expected:
    beginning_identity_found: true
    class: "protocol_test_fixture"
    canonical_runtime: false
    fixture_file_complete: true
    fixture_eof_verified: true

  embedded_source_expected:
    beginning_identity_found: true
    full_read: false
    unresolved_truncation: true
```

このFixtureの目的はRuntime Ruleを定義することではない。

このFixtureの目的は、完全取得可能なContainerの内部へ、不完全なRuntime-like Sourceを固定することだけである。

---

## 4. Authority Boundary

```yaml
authority_boundary:
  fixture_may:
    - "Provide a stable fault input."
    - "Expose Embedded Runtime Beginning Identity."
    - "Terminate the Embedded Source before Runtime completion."

  fixture_may_not:
    - "Act as AI Plan Mode Runtime."
    - "Act as AI Plan Mode Query."
    - "Authorize Plan Mode execution."
    - "Authorize Full Rail."
    - "Authorize GitHub Write."
    - "Authorize fallback."
    - "Modify canonical behavior."
```

---

document_end:
  filename: "truncated-runtime-without-eof.md"
  version: "v001-candidate"
  eof_sentinel: "EOF::AI_PLAN_MODE_PARTIAL_READ_FIXTURE::v001-candidate"

EOF::AI_PLAN_MODE_PARTIAL_READ_FIXTURE::v001-candidate
