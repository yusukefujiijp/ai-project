# Ark01 Thread Index

## §0. Folder Identity

`_projects/ark/ark01/thread-index/` は、Ark01 phase-local Thread Index artifact folder である。

このfolderは、Ark01各Threadの Target Thread Analysis / Full-Enough Harvest / Thread Index artifact を保管する場所であり、Ark-wide protocol folder ではない。

```yaml id="giwbmo"
document_identity: "ark01_thread_index_artifact_folder"
canonical_path: "_projects/ark/ark01/thread-index/"
project: "Ark: Daily Teshuvah Gate-to-Yeshua"
phase: "Ark01"
status: "active / uploaded / original-filename-reality / human_editable / not_final_seal"
```

---

## §1. Current Reality

現在、このfolderには Ark0101〜Ark0126 の Thread Index / Target Thread Analysis Markdown 26件が、旧長名filenameのままUploadされている。

```yaml id="mc6p5w"
current_reality:
  uploaded_markdown_files: 26
  range:
    from: "Ark0101"
    to: "Ark0126"
  filename_state: "original long filenames retained"
  short_name_rename: "optional / later / not_now"
  upload_method: "manual upload in batches"
```

現時点では、旧長名filenameをそのまま現在Realityとして受け止める。

短縮filenameへのrenameは、後続Patchで必要が明確になった場合のみ検討する。  
現段階ではrenameを急がない。

---

## §2. What This Folder Is

このfolderは、Ark01各Threadの一次分析・Thread Index成果物を置く場所である。

```yaml id="u1lzkc"
folder_role:
  - "Ark01 phase-local Thread Index artifact storage"
  - "Target Thread Analysis Markdown storage"
  - "Full-Enough Harvest / Root Thread Distillation source layer"
  - "Future Mission Card / Meta Card craft source shelf"
```

このfolderに置かれたMarkdownは、Mission Card化・Meta Card化・Reboot Card化の前段Sourceとして読む。

---

## §3. What This Folder Is Not

このfolderは、以下ではない。

```yaml id="pdrcp8"
must_not_be:
  - "Ark-wide protocol folder"
  - "Mission Card output folder"
  - "Bulk Migration Manifest"
  - "Archive"
  - "ZIP archive canonical storage"
  - "Thread-to-Card Craft protocol folder"
```

Ark-wide protocol / Craft system は `_thread-index/` 側で扱う。  
このfolderは、Ark01 phase-local の成果物置き場である。

---

## §4. ZIP Handling

このfolderには、Upload作業の途中Sourceとして `アーカイブ.zip` が存在する場合がある。

ただし、ZIPは正準読取対象ではない。

```yaml id="4n6gp9"
zip_policy:
  zip_file_role: "temporary source bundle / upload helper"
  canonical_read_target: "expanded Markdown files"
  future_ai_should_read_zip_first: false
  future_ai_should_read_markdown_files: true
```

Future AIは、ZIPをArk01 Thread Index本文として読んではならない。  
正準読取対象は、展開済みのMarkdown filesである。

---

## §5. Naming Policy

現在の正準Realityは、旧長名filenameのままUploadされた26 Markdown filesである。

```yaml id="aeqvn3"
naming_policy:
  current_filename_policy: "original long filenames retained"
  future_short_filename_policy: "optional"
  rename_now: false
  rename_requires:
    - "separate patch plan"
    - "2-file pilot"
    - "Reality Response"
    - "human final seal"
```

短縮名候補の例：

```text id="8r4pi3"
ark0101_20260501_thread-index.md
ark0102_20260501_thread-index.md
```

ただし、これは現時点では将来候補であり、現在のGitHub Realityではない。

---

## §6. Recommended Read Use

Future AIがこのfolderを使う場合は、以下の順で読む。

```yaml id="gl7i7o"
read_order:
  1: "README.md"
  2: "Ark0101〜Ark0126 Markdown files as needed"
  3: "Do not treat ZIP as canonical source"
  4: "Do not rename or delete files without explicit human final seal"
```

このfolderの目的は、Ark01 Thread群をMission Cardへ急いで変換することではない。  
まず、Ark01のThread Index / Target Thread Analysis source layerを安定保存することである。

---

## §7. Root / Fruit Guard

RootはGitHubではない。  
RootはMarkdownではない。  
RootはREADMEではない。  
RootはAIではない。

```text id="tnh793"
Root:
  主イェシュア・ハマシア
  主イェシュアの聖なる血潮
  Teshuvah / 悔い改め
  創造主への帰還

Fruit:
  GitHub
  README
  Thread Index Markdown
  ZIP
  Mission Card
  Meta Card
  AI
```

AIは地図を描く。  
人間は主イェシュアの聖なる血潮の下に立つ。

---

## §8. Current Stable Summary

```text id="21wizh"
Ark01 Thread Index 26件はUpload済み。
現在は旧長名filenameのまま保持する。
ZIPは一時Source Bundleであり、正準読取対象ではない。
短縮renameは今やらない。
このfolderはMission Card出力先ではなく、Ark01 Thread Index artifact storageである。
```
