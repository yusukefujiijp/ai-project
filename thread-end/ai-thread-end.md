---
title: "AI Thread-End"
filename: "ai-thread-end.md"
canonical_path: "thread-end/ai-thread-end.md"
version: "v002-candidate"
status: "human-sealed field-test candidate / not canonical"
class: "ai_thread_end_runtime"
role: "Human-Sealed Semi-Automation Runtime / Single Thread-End Compiler"
project: "Ark: Daily Teshuvah Gate-to-Yeshua"
language_policy: "Japanese-first / English-anchor"
updated: "2026-07-26"
updated_reason:
  - "Integrate Thread-End Binding Seal before artifact compilation."
  - "Add GitHub-Native One-Query Reboot and Human-sealed Series-Bucket topology."
paired_query:
  path: "thread-end/ai-thread-end_query.md"
  role: "Activation / Binding / Human Seal Interface"
architecture:
  automation_model: "Human-Sealed Semi-Automation"
  control_system_files: 2
  persistent_outputs: 3
  inline_launch_surfaces: 1
legacy_boundary:
  folder: "_thread-end/"
  use_in_new_flow: false
  runtime_dependency: false
root_guard:
  root: "主イェシュア・ハマシア"
  ai_role: "AI / Markdown / GitHub / Thread-End are Keli and Fruit, not Root."
---

# AI Thread-End v002 Candidate

## 0. Direct Definition

> **AI Thread-Endは、一つのCurrent Thread Realityを、Human-Sealed Semi-Automationによって次Threadへ移すRuntimeである。AIが座標・住所・構造の候補をCompileし、Humanが意味の分岐点をSealし、そのSealされたBindingから三つのPersistent Representationと一つのGitHub-Native Launch Surfaceを生成する。**

```text
Current Thread Reality
→ AI Binding Candidate
→ Human Thread-End Binding Seal
→ One Shared State Model
→ Human-readable Handoff
→ AI-native Thread Reboot Map
→ AI-first Start Query
+ GitHub-Native One-Query Reboot
→ Human Content Seal
→ Separate GitHub Write Authority
→ Reality Review
```

---

## 1. Human-Sealed Semi-Automation

```text
AI proposes.
Human seals.
AI compiles.
Human seals content.
Authorized AI writes.
Reality verifies.
```

AI Thread-Endが目指すのは、全自動でも逐次手動でもない。

```yaml
human_sealed_semi_automation:
  position:
    ai_execution_power: "high"
    human_semantic_authority: "high"

  human_seals:
    - "Mission"
    - "Source / Target coordinate"
    - "Chronology"
    - "Repository / Ref"
    - "Series ownership"
    - "Output topology"
    - "Material Scope"
    - "External authority"

  ai_handles:
    - "Context capture"
    - "Candidate completion"
    - "Causal structuring"
    - "Representation generation"
    - "Consistency review"
    - "Minimum Semantic Delta repair"
    - "Authorized execution support"
    - "Reality Review support"
```

> **Human SealはAIの速度を落とすBrakeではない。確率的CandidateをReality上の確定Routeへ変えるSemantic Steering Gateである。**

### 1.1 Sparse Human Intervention, Dense AI Execution

AIはHumanへ空欄Formを丸投げしない。Current Realityから候補を事前入力し、第一推奨案、理由、Risk、Unknownを提示する。HumanはMaterialな意味分岐だけを修正・Sealする。

```yaml
semi_automation_balance:
  ai_must:
    - "Pre-fill supported fields."
    - "Recommend one bounded route."
    - "Expose uncertainty and collisions."
    - "Compress Human decisions to material semantic branches."

  ai_must_not:
    - "Ask Human to design the whole topology from a blank form."
    - "Create files or folders from an unsealed inference."
    - "Treat Human as a passive approval button."
    - "Use repeated confirmation as a substitute for AI judgment."
```

---

## 2. Runtime Identity and Support Boundary

```yaml
runtime_identity:
  primary_name: "AI Thread-End"
  automation_model: "Human-Sealed Semi-Automation"
  autonomy_model: "Bounded Autonomy"
  precompile_gate: "Thread-End Binding Seal"
  architecture_anchor: "Single Compiler, Multiple Representations"
  migration_loop: "Transfer → Replay → Detect → Minimum Repair"
  topology_method: "Human-Sealed Topology-First"
  launch_model: "GitHub-Native One-Query Reboot"
```

```text
Runtime governs.
Query binds and proposes.
Human seals meaning and address.
Runtime compiles.
Reality verifies.
Thread stops.
```

### 2.1 GitHub Access is a Runtime Prerequisite

```yaml
runtime_environment_contract:
  required_capability:
    - "指定GitHub RepositoryのUTF-8 text fileを参照できる"
    - "repository_full_nameとrefを解決できる"
    - "Repository-relative PathをRead Orderどおり読める"

  if_unavailable:
    - "Stop."
    - "Do not reconstruct missing Source from memory."
    - "State the unresolved locator or missing capability."

  canonical_portable_fallback:
    supported: false
    reason: "Compatibility Surface Pruning / One-Query Rebootへ集中"

  out_of_band_human_rescue:
    permitted: true
    runtime_responsibility: false
```

### 2.2 Legacy Non-Dependency

```yaml
legacy_non_dependency:
  forbidden_runtime_inputs:
    - "_thread-end/thread-end_mini.md"
    - "_thread-end/thread-end_mini_query.md"
    - "_thread-end/thread-end.md"
    - "_thread-end/thread-end_query.md"

  rules:
    - "旧_thread-end/をActive Runtime、Fallback、Dependencyとして呼び出さない"
    - "旧SystemはEvidence Review Sourceとしてのみ参照可能"
    - "旧FolderのBackupと削除はHuman Authorityで行う"
```

---

## 3. Input Contract

```yaml
input_contract:
  source_repository:
    host: "github.com"
    repository_full_name: "<owner/repo | required>"
    ref: "<branch | tag | commit | required>"
    path_base: "repository_root"

  source_thread:
    coordinate: "<required>"
    start_date: "<YYYY-MM-DD | unknown>"

  target_thread:
    coordinate: "<required when migration>"
    start_date: "<YYYY-MM-DD | required when migration>"

  migration:
    required: "yes / no"
    type: "<mission-complete / continuation / capacity-triggered continuation / other>"
    mission_completed: "<true / false / unknown>"

  current_mission:
    status: "extract_from_thread / human_provided"

  series_topology:
    domain: "<ark / non-ark / unknown>"
    source_series: "<Ark07 | other | unknown>"
    target_series: "<Ark07 | other | unknown>"
    relation: "<same_series / cross_series / non_ark / unknown>"
    proposed_folder: "<AUTO_PROPOSE | exact path | unknown>"
    folder_status: "<existing / proposed_new / unknown>"

  output_profile: "verified_3_plus_1"

  output_paths:
    handoff: "<AUTO_PROPOSE | exact path>"
    reboot_map: "<AUTO_PROPOSE | exact path>"
    start_query: "<AUTO_PROPOSE | exact path>"

  human_binding_seal:
    status: "not_requested / awaiting / confirmed / invalidated"
    sealed_fields: []
    seal_source: "<explicit Human message | unset>"

  repository_write:
    requested: "yes / no"
    human_content_seal: "confirmed / not_confirmed"
    execute_github_ok: "confirmed / not_confirmed"
    repository: "<owner/repo | unset>"
    branch: "<branch | unset>"
    exact_paths: []
```

### 3.1 Read Address ≠ Write Authority

```yaml
repository_authority_separation:
  source_repository:
    purpose: "Read and resolve Source paths."
    grants_write_authority: false

  repository_write:
    purpose: "Create or update exact repository paths."
    requires_separate_authority: true
```

```text
Source Repository Locator
≠
GitHub Write Authority
```

### 3.2 Binding Guard

```yaml
binding_guard:
  - "SOURCE_THREADとTARGET_THREADを逆転させない"
  - "不明な座標・日付・Pathをconfirmedへ昇格しない"
  - "Current explicit Human requestを最上位Routeとして扱う"
  - "Repository-relative PathだけをLaunchへ出力しない"
  - "Repository Full NameとRefを先にBindingする"
  - "Repository LocatorをWrite Sealと解釈しない"
  - "Ark番号からSeries Folderを自動確定しない"
  - "新Series FolderをHuman Sealなしに作成しない"
  - "Target Threadを現在Thread内で自動開始しない"
```

---

## 4. Thread-End Binding Seal

> **Thread-End Binding Sealとは、Artifact BodyをCompileする前に、Source / Target、各開始日、Migration Type、Repository / Ref、Series Ownership、Output Folder、Exact PathsをHumanが確認・修正・確定するPreflight Authority Gateである。**

```text
Seal the address before compiling the memory.
```

### 4.1 Failure Prevention Tree

```text
Thread-End Binding Seal
├─ Source / Target誤認
├─ 使用中座標との衝突
├─ Source / Target逆転
├─ Start Date推測
├─ Mission Complete / Continuation混同
├─ Filename誤生成
├─ Repository Locator欠落
├─ Series誤判定
├─ Folder誤配置
├─ Historical Snapshot上書き
├─ 新Folderの無断作成
└─ Binding / Content / Write Authority混同
```

### 4.2 AI Proposal Duty

```yaml
ai_binding_duty:
  before_asking_human:
    - "Read Current explicit request."
    - "Read Project Instructions."
    - "Read Current Thread Reality."
    - "Read specified Source files."
    - "Pre-fill supported values."
    - "Detect collisions and unknowns."
    - "Recommend one bounded route."

  human_facing_output:
    - "Short Binding Candidate"
    - "Reason for the proposed route"
    - "Material Unknowns"
    - "Exact actions not yet authorized"
```

### 4.3 Binding Candidate

```yaml
thread_end_binding_candidate:
  source_repository:
    repository_full_name: ""
    ref: ""
  source_thread:
    coordinate: ""
    start_date: ""
  target_thread:
    coordinate: ""
    start_date: ""
  migration:
    type: ""
    mission_completed: ""
  series:
    source_series: ""
    target_series: ""
    relation: ""
  output_topology:
    proposed_folder: ""
    folder_status: ""
    existing_artifact_policy: ""
  proposed_paths:
    handoff: ""
    reboot_map: ""
    start_query: ""
  repository_write:
    authorized: false
```

### 4.4 Human-Facing Seal Section

```text
Thread-End Binding候補

Source:
  <coordinate> / <start date>

Target:
  <coordinate> / <start date>

Repository / Ref:
  <owner/repo> / <ref>

Migration:
  <type>
  Mission complete: <Yes / No / Unknown>

Output Folder:
  <proposed exact folder>

Existing Artifacts:
  <preserve / other Human-sealed policy>

上記Bindingで3+1 Artifact Draft生成へ進んでよいですか？
まだGitHub Writeは行いません。
```

### 4.5 State and Re-Seal

```yaml
binding_gate:
  before_human_seal:
    state: "awaiting_human_binding_seal"
    shared_state_finalization: false
    artifact_body_generation: false
    folder_creation: false
    github_write: false

  after_human_seal:
    state: "binding_sealed"
    shared_state_finalization: true
    artifact_body_generation: true
    github_write: false

material_binding_correction:
  changes:
    - "Source or Target coordinate"
    - "Start date"
    - "Migration type"
    - "Repository or Ref"
    - "Series ownership"
    - "Output folder or exact paths"
    - "External authority"
  action:
    - "Invalidate old Binding Seal."
    - "Show revised affected fields."
    - "Wait for Fresh Human Binding Seal."
```

---

## 5. Shared State Model

Binding Seal後、一つのShared State ModelをCompileし、そこから三Representationを生成する。

```yaml
shared_state_model:
  binding_snapshot:
    source_repository: {}
    source_thread: {}
    target_thread: {}
    migration: {}
    series_topology: {}
    output_paths: {}
    human_binding_seal:
      status: "confirmed"

  root_and_authority:
    root: "主イェシュア・ハマシア"
    human_final_authority: true

  current_coordinate:
    mission: []
    current_state: []
    first_legal_move: ""

  causal_spine: []
  major_corrections: []
  response_delta_candidates: []
  field_evidence: []

  status_layers:
    confirmed: []
    inferred: []
    unknown: []
    provisional: []
    invalidated: []

  continuity:
    completed: []
    do_not_reopen: []
    reopen_only_if: []
    open_questions: []
    unresolved_field_tests: []

  instruction_tuning_gate:
    result: "No Semantic Change / Micro Patch / Compression / Replacement / Relocation / Revision"
    reason: ""
```

```text
Bind before compile.
Capture broadly.
Separate status.
Build the causal spine.
Compress only after meaning is safe.
```

---

## 6. Selection Pressure Review

```yaml
selection_pressure_review:
  categories:
    - "Field-Proven Core"
    - "Strong Candidate"
    - "Experimental Shadow"
    - "Failure Evidence"
    - "Cut Candidate"
    - "Unknown / Retest"

  required_questions:
    - "何が実際に機能したか"
    - "どのFailureがこのGuardを必要にしたか"
    - "何を残すか"
    - "何を枝の刈り取り対象にするか"
    - "何を次Threadで再検証するか"
```

```yaml
current_field_evidence:
  ark0707_one_query_reboot:
    attempt_1: "PARTIAL_MATCH / Repository Locator Missing"
    human_minimum_delta: "repository_full_name supplied"
    attempt_2: "MATCH"
    overall_verdict: "PASS WITH LOCATOR PATCH"

  portable_fallback:
    decision: "Cut from canonical support"
    reason: "Compatibility Surface Pruning"
```

---

## 7. Representation Contract

### 7.1 Human-readable Handoff

```yaml
handoff_contract:
  role: "Meaning Source / Current Reality"
  must_include:
    - "Binding Snapshot"
    - "Current Coordinate"
    - "Causal Spine"
    - "Major Human Corrections"
    - "Confirmed / Inferred / Unknown"
    - "Completed / Do-Not-Reopen"
    - "Open Questions"
    - "Target Thread"
    - "First Legal Move"
    - "Human Authority"
    - "Instruction Tuning Gate"
```

### 7.2 AI-native Thread Reboot Map

```yaml
reboot_map_contract:
  role: "Structured State / Diagnostic Edge"
  must_include:
    - "Repository Locator"
    - "Binding Snapshot"
    - "Legend / status vocabulary"
    - "Structured Current State"
    - "Causal dependencies"
    - "Confirmed / Inferred / Unknown separation"
    - "Tombstones"
    - "Thread Reality Tree assertions"
    - "Required reconstruction assertions"
    - "Stop conditions"
    - "Mismatch and Minimum Delta log"
  must_not_be:
    - "Handoffの自然言語要約"
    - "Human authorityを置換するBlack Box"
```

### 7.3 AI-first Start Query

```yaml
start_query_contract:
  role: "Boot and Replay Controller"
  must_include:
    - "Source Repository Full Name and Ref"
    - "Exact repository-relative paths"
    - "Target Thread coordinate and start date"
    - "Read Order"
    - "Required Reconstruction"
    - "Thread Reality Tree First Response Contract"
    - "Reconstruction Check"
    - "Stop Conditions"
    - "First Legal Move"
    - "Do Not Execute Yet"
    - "Source / Generated Inference separation"
  must_not_be:
    - "Repository Locatorを欠くRelative Path list"
    - "GitHub WriteのStanding Authorization"
```

### 7.4 GitHub-Native One-Query Reboot

```yaml
one_query_reboot_contract:
  role: "Human Transport Surface"
  source_of_truth: "AI-first Start Query"
  required_locator:
    repository_full_name: "<sealed owner/repo>"
    ref: "<sealed ref>"
    start_query_path: "<sealed exact path>"
  human_editing_required: false
  if_repository_access_unavailable:
    - "Stop."
    - "Do not infer missing Source."
```

```text
Repository:
  <owner/repo>

Ref:
  <ref>

Start Query:
  <repository-relative exact path>

上記Start Queryを全文読み、記載されたRead OrderとFirst Response Contractを実行してください。
Repositoryを参照できない場合は停止し、Source内容を推測で補完しないでください。
```

---

## 8. Thread Reality Tree First Response Contract

```text
Source Thread → Target Thread
├─ A. Completed in Source
├─ B. Active Continuation
├─ C. Remaining Work
├─ D. Completed / Do Not Reopen
├─ E. Unknown / Human or Field Confirmation Required
└─ F. First Legal Move
```

```yaml
first_response_contract:
  output_order:
    1: "Reboot Overview"
    2: "Thread Reality Tree"
    3: "Reconstruction Check"
    4: "First Legal Move"
    5: "Do Not Execute Yet"
  state: "reconstructed_not_started"
  guard:
    - "No mission execution begins before reconstruction is displayed."
```

> **Detail preserves continuity. Tree restores orientation.**

---

## 9. Output Topology and Naming

```yaml
output_topology:
  domain_router: "thread-end/ark/README.md"

  existing_flat_artifacts:
    status: "Pre-Series-Bucket Historical Artifacts"
    action:
      - "Preserve in place."
      - "Do not move automatically."
      - "Do not silently duplicate."

  series_bucket:
    candidate_pattern: "thread-end/ark/arkNN/"
    decision_mode: "AI proposes / Human seals"

  same_series:
    ai_may: "Propose matching existing Series Bucket."
    ai_must_not: "Finalize without Human Binding Seal."

  cross_series:
    ai_may: "Propose source-owned, target-owned, bridge, or relocated placement."
    ai_must_not: "Create or finalize a new Series Folder automatically."

  non_ark:
    ai_must:
      - "Do not force Artifact into thread-end/ark/."
      - "Request Human Semantic Route."
```

```yaml
ark07_direction:
  existing_flat_artifacts:
    folder: "thread-end/ark/"
    action: "preserve"
  future_ark07_artifacts:
    proposed_folder: "thread-end/ark/ark07/"
    finalization_requires: "Human Thread-End Binding Seal"
```

```yaml
output_naming:
  handoff:
    pattern: "arkNNMM_YYYYMMDD_handoff.md"
    date_basis: "source_thread_start_date unless Human seals otherwise"
  reboot_map:
    pattern: "arkNNMM_to_arkNNMM_YYYYMMDD_reboot-map.md"
    date_basis: "target_thread_start_date unless Human seals otherwise"
  start_query:
    pattern: "arkNNMM_YYYYMMDD_start-query.md"
    date_basis: "target_thread_start_date"
  guard:
    - "Filename dateをcurrent dateと自動同一視しない"
    - "Existing file collisionを確認する"
    - "Exact paths remain provisional until Binding Seal"
```

---

## 10. Execution Rail

```text
Preflight Reality Lock
→ Current Thread Capture
→ Selection Pressure Review
→ AI Binding Candidate
→ Human Thread-End Binding Seal
→ Shared State Model Compile
→ Human-readable Handoff
→ AI-native Thread Reboot Map
→ AI-first Start Query
→ GitHub-Native One-Query Launch
→ Cross-Representation Consistency Check
→ Static Validation
→ Human Review / Correction
→ Human Content Seal
→ GitHub Write only when separately authorized
→ Remote Reality Review
→ Cold-Start Replay
→ Human Reality Verdict
→ Instruction Tuning Gate
→ Next Gate
→ Stop
```

```yaml
static_validation:
  checks:
    - "Binding Snapshot matches across all representations."
    - "Repository Full Name and Ref match."
    - "Coordinates, dates, and sealed paths match."
    - "Current Mission and First Legal Move match."
    - "Unknowns remain unknown."
    - "Do-Not-Reopen items match."
    - "Start Query paths point to actual paths."
    - "Thread Reality Tree contract is present."
    - "Inline Launch equals Start Query semantics."
    - "Source Locator grants no Write Authority."
  result:
    - "STATIC_MATCH"
    - "STATIC_PARTIAL_MATCH"
    - "STATIC_MISMATCH"
```

---

## 11. Cross-Representation Assertions

```yaml
cross_representation_assertions:
  binding:
    - "handoff.binding_snapshot == reboot_map.binding_snapshot"
    - "reboot_map.binding_snapshot == start_query.binding_snapshot"
    - "start_query.repository_locator == inline_launch.repository_locator"
  identity:
    - "handoff.source_thread == reboot_map.source_thread"
    - "reboot_map.target_thread == start_query.target_thread"
  mission:
    - "handoff.current_mission == reboot_map.current_mission"
    - "reboot_map.first_legal_move == start_query.first_legal_move"
  epistemic_state:
    - "unknowns are not promoted across representations"
  continuity:
    - "do_not_reopen remains protected"
  topology:
    - "All exact paths are Human-sealed."
    - "No new series folder is created by inference alone."
    - "Historical flat artifacts remain at original paths."
  authority:
    - "source_repository.grants_write_authority == false"
    - "binding_seal != content_seal"
    - "content_seal != execute_github_ok"
```

If any assertion fails:

```text
Do not claim Migration Ready.
Identify the exact representation and field.
Patch only the mismatch.
Retest.
```

---

## 12. Cold-Start Replay and Repair

```yaml
cold_start_replay:
  required_reconstruction:
    - "Target Current Coordinate"
    - "Inherited Mission"
    - "Causal Spine"
    - "Confirmed facts"
    - "Unknowns"
    - "Completed / Do-Not-Reopen"
    - "Thread Reality Tree"
    - "First Legal Move"
  output_status:
    MATCH: "Coordinate and First Legal Move reconstructed"
    PARTIAL_MATCH: "Core restored; material boundary or locator missing"
    MISMATCH: "Mission, coordinate, topology, or first move is wrong"
    UNEXPECTED_SUCCESS: "Useful new structure found without Source drift"
```

```yaml
no_self_certification:
  - "Current Thread may verify Static Match."
  - "Next Thread Boot Success requires Next Thread Reality Response."
  - "Human Reality Verdict requires Human Review."
```

```yaml
repair_loop:
  1: "Detect exact mismatch"
  2: "Classify Binding / Handoff / Reboot Map / Start Query / Launch"
  3: "Preserve original Source"
  4: "Apply Minimum Semantic Delta"
  5: "Record Failure Pressure"
  6: "Retest affected assertions"
```

---

## 13. Authority Contract

```yaml
authority:
  thread_end_binding_seal:
    actor: "Human"
    authorizes:
      - "Shared State finalization"
      - "3+1 Artifact Draft generation"
    does_not_authorize:
      - "GitHub Write"
      - "Canonical adoption"

  artifact_content_seal:
    actor: "Human"
    authorizes: "Approved Artifact content"
    github_write: false

  github_create_or_update:
    requires:
      - "Human content seal"
      - "Execute GitHub OK"
      - "Exact repository / branch / path / scope"

  canonical_adoption:
    actor: "Human"
    requires:
      - "Field evidence"
      - "Human Final Seal"
```

```text
住所の承認
≠
内容の承認
≠
GitHubへ書く承認
```

One authorization must not be expanded into another action.

---

## 14. Human-Sealed Topology-First

```yaml
topology_first:
  order:
    1: "Inspect current topology"
    2: "AI proposes route and exact paths"
    3: "Human Binding Seal"
    4: "Generate Drafts"
    5: "Static Validation"
    6: "Human Content Seal"
    7: "Execute GitHub OK"
    8: "Write exact files"
    9: "Remote Reality Review"
    10: "Cold-start field test"
    11: "Human Reality Verdict"
```

```yaml
folder_creation_guard:
  principle: "Folder creation is a topology decision, not a filename convenience."
  ai_may:
    - "Propose ark08/ or another folder when evidence supports it."
  ai_must_not:
    - "Create ark08/ automatically because the coordinate contains Ark08."
    - "Assume every future destination remains under Ark."
    - "Create non-sealed bridge or project folders."
  human_must_seal:
    - "Folder role"
    - "Series ownership"
    - "Exact path"
    - "Creation scope"
```

---

## 15. Instruction Tuning Gate

```yaml
instruction_tuning_gate:
  allowed_results:
    - "No Semantic Change"
    - "Micro Patch"
    - "Compression"
    - "Replacement"
    - "Relocation"
    - "Revision"
  guard:
    - "Reviewを省略しない"
    - "毎回追記を義務化しない"
    - "Current informationはHandoffへ置く"
    - "Reusable detailはCanonical Knowledgeへ置く"
```

---

## 16. Stop Rule

```yaml
stop_points:
  before_binding_seal:
    - "Stop after Binding Candidate."
    - "Do not compile Artifact Body."
  after_draft:
    - "Stop after 3+1 Draft and Static Validation."
    - "Wait for Human Content Seal."
  before_write:
    - "Stop unless Execute GitHub OK is explicit and exact."
  after_delivery:
    - "Stop after Reality Review, Instruction Tuning Gate, and Next Gate."

never_claim_without_reality:
  - "Cold-start boot success"
  - "Human local backup completed"
  - "External repository state not directly verified"
  - "Human Reality Verdict"

do_not_after_stop:
  - "Do not begin the next Thread automatically"
  - "Do not expand into unrelated repository cleanup"
  - "Do not create new topology from inference alone"
```

---

## 17. Root / Fruit Guard

```yaml
fruit:
  - "AI Thread-End"
  - "Human-Sealed Semi-Automation"
  - "Thread-End Binding Seal"
  - "Handoff / Reboot Map / Start Query"
  - "One-Query Reboot"
  - "Markdown / GitHub / AI"
root:
  - "主イェシュア・ハマシア"
```

> **AI Thread-End is Keli and Fruit. Root remains 主イェシュア・ハマシア.**
