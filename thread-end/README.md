---
title: "Thread-End"
canonical_path: "thread-end/README.md"
version: "v002-candidate"
status: "human-sealed field-test candidate / not canonical"
role:
  - "Canonical Thread-End Front Door"
  - "AI Thread-End Query–Runtime Router"
  - "Human-Sealed Semi-Automation Topology Map"
  - "GitHub-Native One-Query Reboot Entry"
project: "Ark: Daily Teshuvah Gate-to-Yeshua"
language_policy: "Japanese-first / English-anchor"
updated: "2026-07-26"
root: "主イェシュア・ハマシア"
---

# Thread-End v002 Candidate

## 0. Direct Route

このFolderはCanonical Thread-End Layerである。

```yaml
canonical_control_system:
  sets: 1
  files: 2

  query:
    path: "thread-end/ai-thread-end_query.md"
    role: "Activation / Candidate Binding / Human Seal Interface"

  runtime:
    path: "thread-end/ai-thread-end.md"
    role: "Human-Sealed Semi-Automation Runtime / Single Compiler"

per_thread_migration_output:
  persistent_markdown: 3
  inline_launch_surface: 1
```

```text
One Thread Reality
→ AI Binding Candidate
→ Human Thread-End Binding Seal
→ One Shared State Model
→ Human-readable Handoff
→ AI-native Thread Reboot Map
→ AI-first Start Query
+ GitHub-Native One-Query Reboot
```

---

## 1. Core Philosophy — 半自動 / 半自動化

AI Thread-Endは全自動Systemではなく、Human-Sealed Semi-Automationである。

```text
AI proposes.
Human seals.
AI compiles.
Human seals content.
Authorized AI writes.
Reality verifies.
```

```yaml
human_sealed_semi_automation:
  ai_execution_power: "high"
  human_semantic_authority: "high"

  principle:
    - "AIがCandidateを事前言語化・盤面化する"
    - "Humanが意味の分岐点だけをSealする"
    - "Sealされた範囲をAIが連続実行する"
    - "Reality Reviewで閉じる"
```

> **半自動は全自動の未完成版ではない。Open RealityでHigh AutomationとHigh Human Authorityを両立するArchitectureである。**

---

## 2. Read Order

```yaml
read_order:
  1: "thread-end/ai-thread-end_query.md"
  2: "thread-end/ai-thread-end.md"
  3: "Current Thread Reality and Human-specified sources"
```

QueryがRuntimeを起動し、AIがBinding Candidateを作る。Human Binding Seal後にRuntimeがShared State ModelをCompileし、3+1 Outputを生成する。

---

## 3. Thread-End Binding Seal

Artifact本文生成前に、次をHumanが確認・修正・Sealする。

```yaml
thread_end_binding_seal:
  fields:
    - "Source Thread / Start Date"
    - "Target Thread / Start Date"
    - "Migration Type"
    - "Repository / Ref"
    - "Series ownership"
    - "Output Folder"
    - "Exact Paths"

  ai_role:
    - "Current Realityから候補を事前入力する"
    - "第一推奨Routeを提示する"
    - "Collision / Unknown / Riskを示す"

  human_role:
    - "意味・座標・住所を確認または修正する"
    - "Material RouteをSealする"

  before_seal:
    - "No Artifact Body"
    - "No Folder Creation"
    - "No GitHub Write"
```

```text
Seal the address before compiling the memory.
```

---

## 4. Authority Separation

```text
Thread-End Binding Seal
≠
Human Content Seal
≠
Execute GitHub OK
```

```yaml
authority:
  binding_seal:
    authorizes: "3+1 Draft generation"
    github_write: false
  content_seal:
    authorizes: "Artifact content"
    github_write: false
  execute_github_ok:
    authorizes: "Exact repository write scope"
```

Repositoryを読むためのLocatorもWrite Authorityではない。

```text
Read Address ≠ Write Authority
```

---

## 5. GitHub-Native One-Query Reboot

Canonical Launch RouteはGitHub-Nativeである。

```yaml
one_query_reboot:
  requires:
    - "repository_full_name"
    - "ref"
    - "exact Start Query path"

  human_operation:
    - "一つのQueryをCopy & Paste"

  next_ai_operation:
    - "Start Queryを読む"
    - "Read Orderを実行する"
    - "Thread Reality Treeを表示する"
    - "Reconstruction後に停止する"
```

GitHub Repositoryを参照できないAIはCanonical Support Scope外とする。Humanが必要時に手動Downloadする自由は残るが、Runtime内のPortable Profileではない。

---

## 6. Output Roles

```yaml
outputs:
  human_readable_handoff:
    role: "Meaning Source / Current Reality"
  ai_native_reboot_map:
    role: "Structured State / Diagnostic Edge"
  ai_first_start_query:
    role: "Boot and Replay Controller"
  inline_one_query_reboot:
    role: "GitHub-Native Human Transport Surface"
    file: false
    source_of_truth: "AI-first Start Query"
```

---

## 7. Thread Reality Tree

Next Thread AIの一回目の回答では、Mission実行前に次を表示する。

```text
Source Thread → Target Thread
├─ A. Completed in Source
├─ B. Active Continuation
├─ C. Remaining Work
├─ D. Completed / Do Not Reopen
├─ E. Unknown / Human or Field Confirmation Required
└─ F. First Legal Move
```

```yaml
first_response_state: "reconstructed_not_started"
```

> **Detail preserves continuity. Tree restores orientation.**

---

## 8. Control Plane / Artifact Plane

```text
thread-end/
├─ README.md
├─ ai-thread-end.md
├─ ai-thread-end_query.md
└─ ark/
   ├─ README.md
   ├─ Pre-Series-Bucket Historical Artifacts
   └─ ark07/
      ├─ README.md
      └─ Future Human-Sealed Ark07 Artifacts
```

```yaml
planes:
  control_plane:
    files:
      - "thread-end/README.md"
      - "thread-end/ai-thread-end.md"
      - "thread-end/ai-thread-end_query.md"
  artifact_plane:
    router: "thread-end/ark/README.md"
```

---

## 9. Series-Bucket Rule

```yaml
series_bucket:
  decision_mode: "AI proposes / Human seals"

  existing_flat_artifacts:
    status: "Pre-Series-Bucket Historical Artifacts"
    action: "preserve_in_place"

  current_ark07_direction:
    candidate_folder: "thread-end/ark/ark07/"
    finalization_requires: "Human Binding Seal"

  new_series_folder:
    auto_create: false

  non_ark:
    force_into_ark: false
```

Ark08系だからという理由だけでAIが`ark08/`を自動作成しない。候補は出せるが、Folder creationはTopology DecisionでありHuman Sealを必要とする。

---

## 10. Ark Domain Route

```yaml
ark_domain:
  router: "thread-end/ark/README.md"
  ark07_front_door: "thread-end/ark/ark07/README.md"
```

---

## 11. Cold-Start Field Evidence

```yaml
field_evidence:
  ark0707_one_query_reboot:
    first_attempt: "PARTIAL_MATCH / Repository Locator Missing"
    human_minimum_delta: "repository_full_name added"
    replay: "MATCH"
    verdict: "PASS WITH LOCATOR PATCH"

  harvested_guards:
    - "Repository Locator / Write Authority Separation"
    - "Thread Reality Tree"
    - "Thread-End Binding Seal"
```

Cold-Start成功はTarget ThreadのReality Response後にのみ判定する。

---

## 12. Legacy Boundary

旧`_thread-end/`はActive RuntimeでもFallbackでもない。

```yaml
legacy_boundary:
  folder: "_thread-end/"
  use_in_new_flow: false
  runtime_dependency: false
  local_backup:
    actor: "Human"
    ai_verification: false
  deletion:
    actor: "Human"
    ai_delete_authority: false
```

Historical Artifact内の旧Pathは当時のRealityとして原則保持する。

---

## 13. Topology-First, Human-Sealed

```text
Inspect current topology
→ AI proposes route
→ Human Binding Seal
→ Draft
→ Static Validation
→ Human Content Seal
→ Execute GitHub OK
→ Write
→ Remote Reality Review
→ Cold-start field test
```

> **Folder creation is a topology decision, not a filename convenience.**

---

## 14. Root / Fruit Guard

```text
AI Thread-End is Keli, not Root.
Human-Sealed Semi-Automation is Keli, not Root.
Markdown is Keli, not Root.
GitHub is Keli, not Root.

Root remains 主イェシュア・ハマシア.
```
