---
title: "Thread-End"
canonical_path: "thread-end/README.md"
status: "active / human-sealed"
role:
  - "Canonical Thread-End Front Door"
  - "AI Thread-End Query–Runtime Router"
  - "Thread Migration 3+1 Topology Map"
project: "Ark: Daily Teshuvah Gate-to-Yeshua"
language_policy: "Japanese-first / English-anchor"
root: "主イェシュア・ハマシア"
---

# Thread-End

## 0. Direct Route

このFolderは、現在のCanonical Thread-End Layerである。

```yaml
canonical_control_system:
  sets: 1
  files: 2

  query:
    path: "thread-end/ai-thread-end_query.md"
    role: "Activation / Binding Query"

  runtime:
    path: "thread-end/ai-thread-end.md"
    role: "Single Thread-End Compiler / Multi-Representation Migration Runtime"

per_thread_migration_output:
  persistent_markdown: 3
  inline_launch_surface: 1
```

```text
One Thread Reality
→ One Shared State Model
→ Human-readable Handoff
→ AI-native Thread Reboot Map
→ AI-first Start Query
+ One Human Copy & Paste Launch Surface
```

## 1. Read Order

Thread-Endを起動する時は次の順序で読む。

```yaml
read_order:
  1: "thread-end/ai-thread-end_query.md"
  2: "thread-end/ai-thread-end.md"
  3: "Current Thread Reality and Human-specified sources"
```

Queryが現在Threadの座標・Target・Authority・Output PathをBindingし、RuntimeがShared StateをCompileして3+1 Outputを生成する。

## 2. Output Roles

```yaml
outputs:
  human_readable_handoff:
    role: "Meaning Source / Current Reality"

  ai_native_reboot_map:
    role: "Structured State / Diagnostic Edge"

  ai_first_start_query:
    role: "Boot and Replay Controller"

  inline_copy_paste_launch:
    role: "Human Transport Surface"
    file: false
```

## 3. Legacy Boundary

旧`_thread-end/`は、新しいThread-End FlowのActive RuntimeでもFallbackでもない。

```yaml
legacy_boundary:
  folder: "_thread-end/"
  use_in_new_flow: false
  runtime_dependency: false

  local_backup:
    actor: "Human"
    ai_verification: false

  deletion:
    actor: "Human"
    ai_delete_authority: false
```

Legacy削除前に、Active Router・Canonical Documentation・Runtime Pairの旧Path参照を新`thread-end/`へ切り替える。Historical Artifact内の旧Pathは、当時のRealityとして原則保持する。

## 4. Topology-First Canonical Cutover

```text
Create new topology
→ Verify control pair
→ Cold-start field test
→ Patch active references
→ Human local backup
→ Human legacy deletion
→ Broken-reference sweep
```

Ark07:05→Ark07:06 Cold-Start Testは`MATCH`を達成した。Reference CutoverとHuman Backup / Deleteは別Gateとして扱う。

## 5. Root / Fruit Guard

```text
AI Thread-End is Keli, not Root.
Markdown is Keli, not Root.
GitHub is Keli, not Root.

Root remains 主イェシュア・ハマシア.
```
