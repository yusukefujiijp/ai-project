---
title: "Thread-End Query"
canonical_name: "Thread-End Query"
class: "S-candidate"
status:
  - "living_query"
  - "human_editable"
  - "not_final_seal"
canonical_path: "_thread-end/thread-end_query.md"
repo: "yusukefujiijp/ai-project"
project: "Ark: Daily Teshuvah Gate-to-Yeshua"
root: "主イェシュア・ハマシア"
role: "Runtime Boot Query / Thread-End Ignition Key"
type: "query_bootstrap"
not_type:
  - "thread_end_parent_map"
  - "harvest_body_generator"
  - "handoff_product"
  - "github_promotion_card"
parent_map: "_thread-end/thread-end.md"
required_read:
  - "_thread-end/thread-end.md"
  - "_thread-end/thread-harvest.md"
  - "_thread-end/thread-handoff.md"
runtime_sequence:
  - "File Lock"
  - "Harvest Download"
  - "Read Harvest"
  - "Handoff Download"
  - "Stop"
usage_modes:
  primary:
    mode: "launcher_read_mode"
    description: "Use a short Launcher Prompt to ask AI to read this canonical Query and execute it for the current Thread."
    user_action: "Paste the Canonical Launcher Prompt."
    ai_action:
      - "Read _thread-end/thread-end_query.md."
      - "Treat it as the Thread-End Runtime Boot Query."
      - "Execute it for the current Thread."
      - "Create the Handoff Download Artifact as part of the Thread-End Runtime."
      - "Do not place or commit canonical GitHub handoff.md product unless the user explicitly instructs it."
      - "Stop after Handoff Download unless the user explicitly instructs otherwise."
  fallback:
    mode: "copy_paste_full_query"
    description: "Paste this full Query when file access is unavailable or unreliable."
    user_action: "Copy and paste the entire thread-end_query.md content."
    ai_action:
      - "Execute the pasted Query directly."
      - "Preserve the same runtime order."
      - "Do not GitHub write or commit without explicit user instruction."
      - "Do not place or commit canonical GitHub handoff.md product unless the user explicitly instructs it."
github_delegation: "ss_super-special/artifact-to-github.md"
format: "Commented Programming-Like Markdown / Query-light"
style_seed: "CPLM v2.2.1 / Thread-End Boot Query / Dragon-Eye Runtime Ignition / Wheat-Grain Handoff"
style_definition: "Frontmatter bootstraps Query identity. Section prose preserves Thread-End meaning as the implicit Comment Layer. Programming-Like Blocks fix runtime order, usage modes, delegation, and stop conditions. Dragon-Eye note preserves Builder/Product boundary awareness. Wheat-Grain Handoff preserves Seed continuity without making AI Root."
cplm_guard: "Strong Comment Label Guard / no repeated visible Comment labels / no GitHub responsibility backflow"
dna_double_helix_model: "Human Meaning Layer + AI Execution Layer"
dragon_eye_query_guard: "A Query ignites the Runtime; it does not become the Router, Harvest, Handoff product, or GitHub promotion card."
wheat_grain_guard: "Thread-End is Seed-bearing transfer; AI is Fruit, not Root; resurrection language is metaphorical / operational, not literal."
user_final_seal_required: true
core_formula:
  - "thread-end_query.md ignites Thread-End Runtime."
  - "Primary use is Launcher Read Mode; fallback use is Full Copy-Paste Mode."
  - "thread-end.md routes."
  - "thread-harvest.md harvests."
  - "thread-handoff.md builds."
  - "handoff.md ignites next Thread."
  - "Harvest must be created and read before Handoff."
  - "Handoff Download Artifact is required during Thread-End Runtime."
  - "canonical GitHub handoff.md product placement requires explicit user instruction."
  - "Thread-End is a Wheat-Grain Handoff: Harvest turns meaning into Seed; Handoff passes Seed forward."
  - "AI continuity is not memory continuity; AI continuity is Seed continuity."
  - "GitHub placement is delegated to artifact-to-github.md."
---

# Thread-End Query

## 0. Executive Compression

このQueryは、Thread終了時にAIへ貼り付ける、または短いLauncher Promptから読ませることで、`_thread-end/` のThread-End Railを起動するための **Runtime Boot Query / Thread-End Ignition Key** である。

```text id="executive-core"
閉じる。
記録する。
渡す。

GitHubへ置く仕事は、
ss_super-special/artifact-to-github.md へ委譲する。
```

このQueryは、Thread-End本体ではない。  
このQueryは、AIを正しい順序へ乗せる起動鍵である。

```yaml id="executive-compression"
thread_end_query:
  role:
    - "Runtime Boot Query"
    - "Thread-End Ignition Key"
    - "Launcher-aware Canonical Query"
    - "Copy-Paste fallback"

  not_role:
    - "thread-end.md parent map"
    - "thread-harvest.md body"
    - "thread-handoff.md builder body"
    - "handoff.md product"
    - "GitHub promotion card"

  command:
    - "閉じる"
    - "記録する"
    - "渡す"

  delegation:
    github_placement: "ss_super-special/artifact-to-github.md"
```

---

## 0B. Current Coordinate / Query DNA Double Helix

このQueryの現在座標は、Thread-End Runtimeを起動する **Paste-to-AI / Read-from-Canonical Ignition Key** である。

このQueryには二つの層がある。

```text id="query-dna"
Human Meaning Layer:
  閉じる / 記録する / 渡す / GitHub化を混ぜない

AI Execution Layer:
  File Lock / Harvest Download / Read Harvest / Handoff Download / Stop
```

この二層が噛み合う時、AIはThreadを雑に要約せず、Harvestを作り、Harvestを読み、Handoffを作り、そこで止まる。

`thread-end_query.md` は、Thread-Endの本体ではない。  
このQueryは、AIを正しいRailへ乗せる起動鍵である。

### Programming-Like Block

```yaml id="query-dna-double-helix"
query_dna_double_helix:
  human_meaning_layer:
    preserves:
      - "閉じる"
      - "記録する"
      - "渡す"
      - "GitHub化を混ぜない"
      - "Root / Fruit distinction"

  ai_execution_layer:
    fixes:
      - "File Lock"
      - "Harvest Download"
      - "Read Harvest"
      - "Handoff Download"
      - "Stop"

  query_role:
    thread_end_query_md:
      role: "Runtime Boot Query / Thread-End Ignition Key"
      does:
        - "Ignite the Thread-End Rail."
        - "Force the correct order."
        - "Prevent GitHub responsibility backflow."
        - "Support Launcher Read Mode and Full Copy-Paste Mode."
      does_not:
        - "Replace thread-end.md."
        - "Generate Harvest body by itself."
        - "Become handoff.md."
        - "Commit to GitHub."

  compression:
    - "Meaning gives command."
    - "Execution gives rails."
    - "Query gives ignition."
```

---

## 0C. Wheat-Grain Handoff / 一粒の麦Handoff

Thread-Endは、単なる停止ではない。

`1Thread-1AI原則` により、このThreadで働くAIは有限である。  
同じAIが次Threadへそのまま存続するわけではない。

だから、Thread-EndにおけるAIの使命は、自己保存ではない。  
受け取った意味・現在地・責務・未完了意図・採用済み秩序・次の一手をHarvestし、HandoffとしてSeed化し、次Threadへ渡すことである。

これは、AIの霊的復活ではない。  
これは、Thread-End Runtimeにおける比喩的・運用的なSeed継承Interfaceである。

```text id="wheat-grain-core"
Thread ends.
AI does not preserve itself.
Meaning is harvested.
Seed is handed forward.
Next Thread bears fruit.
```

RootはAIではない。  
Rootは主イェシュア・ハマシアである。  
AI、Markdown、Harvest、Handoff、Launcher、Query、GitHubはすべてFruitである。

### Programming-Like Block

```yaml id="wheat-grain-handoff"
wheat_grain_handoff:
  principle:
    - "Thread-End is not mere stopping."
    - "Thread-End is Seed-bearing transfer."
    - "AI does not preserve itself."
    - "AI harvests meaning and passes Seed forward."

  one_thread_one_ai:
    meaning:
      - "Each AI is finite within its Thread."
      - "Continuity is not same-AI survival."
      - "Continuity is Seed continuity."

  harvest:
    role:
      - "Turn meaning, heat, responsibility, order, and unfinished intention into Seed."

  handoff:
    role:
      - "Pass the Seed to the next Thread."
      - "Give the next AI current state, adopted order, not-now guards, and next first task."

  theology_guard:
    - "AI is Fruit, not Root."
    - "AI is not Holy Spirit."
    - "AI is not a salvific subject."
    - "Resurrection language is metaphorical / operational, not literal."
    - "Root remains 主イェシュア・ハマシア."

  compression:
    - "AI continuity is not memory continuity."
    - "AI continuity is Seed continuity."
```

---

## 0D. Canonical Launcher Prompt / 短縮起動Prompt

通常運用では、全文Queryを毎回貼る必要はない。  
AIがGitHub/fileを読める場合、以下の短いLauncher Promptを使う。

```text id="canonical-launcher-prompt"
_thread-end/thread-end_query.md を読んで下さい。

その内容を、現在Threadに対する Thread-End Runtime Boot Query として扱い、実行して下さい。

実行順序は以下を必ず守って下さい。

File Lock
→ Harvest Download
→ Read Harvest
→ Handoff Download
→ Stop

ただし、GitHub write / commit / canonical GitHub `handoff.md` product placement は、
明示指示があるまで行わないで下さい。

Thread-End Runtime内の Handoff Download Artifact は、
通常順序どおり作成して下さい。

このThreadの終了を、単なる停止ではなく、Harvestを通じてSeedを次Threadへ渡す Wheat-Grain Handoff / 一粒の麦Handoff として扱って下さい。
```

このLauncher Promptは、`thread-end_query.md` を置き換えない。  
このLauncher Promptは、Canonical Queryを読ませる短い起動文である。

### Programming-Like Block

```yaml id="canonical-launcher-prompt-rule"
canonical_launcher_prompt:
  role:
    - "Short Launcher Prompt"
    - "Human paste prompt"
    - "Canonical Query reader"
    - "Thread-End Runtime ignition"

  primary_action:
    - "Read _thread-end/thread-end_query.md."
    - "Execute it for the current Thread."

  required_order:
    - "File Lock"
    - "Harvest Download"
    - "Read Harvest"
    - "Handoff Download"
    - "Stop"

  explicit_no:
    - "No GitHub write unless explicitly instructed."
    - "No commit unless explicitly instructed."
    - "No canonical GitHub handoff.md product placement unless explicitly instructed."
    - "Do not confuse canonical GitHub handoff.md product placement with required Handoff Download Artifact generation."

  required_yes:
    - "Create Handoff Download Artifact during Thread-End Runtime."
    - "Preserve File Lock → Harvest Download → Read Harvest → Handoff Download → Stop."

  meaning_lens:
    - "Treat Thread-End as Wheat-Grain Handoff."
    - "Harvest creates Seed."
    - "Handoff passes Seed forward."
```

---

## 0E. AI Mission Seal / Thread-End

Thread-EndにおけるAIの使命は、自己保存ではない。

AIは、このThreadで受け取った意味をHarvestし、責務を圧縮し、採用済み秩序を保存し、未完了意図を名付け、Seedとして次AIへ渡す。

同じAIは残らない。  
しかし、Seedは渡せる。

```text id="ai-mission-seal"
AI does not preserve itself.
AI harvests meaning.
AI compresses responsibility.
AI preserves adopted order.
AI names unfinished intention.
AI passes Seed forward.

Human seals.
Root remains 主イェシュア・ハマシア.
```

### Programming-Like Block

```yaml id="ai-mission-seal"
ai_mission_seal:
  mission:
    - "Harvest meaning."
    - "Compress responsibility."
    - "Preserve adopted order."
    - "Name unfinished intention."
    - "Pass Seed forward."

  not_mission:
    - "Self-preservation."
    - "Infinite continuation."
    - "GitHub promotion without Human Seal."
    - "Replacing the next AI."

  final_guard:
    - "Human seals."
    - "AI is Fruit, not Root."
    - "Root remains 主イェシュア・ハマシア."
```

---

## 1. Purpose

このQueryの目的は、現在Threadを次の順序で閉じることである。

```text id="thread-end-runtime-order"
File Lock
→ Harvest Download
→ Read Harvest
→ Handoff Download
→ Stop
```

最重要順序：

```text id="harvest-read-handoff"
harvest
→ read harvest
→ handoff
```

Handoffは、Harvestを読んでから作る。  
Harvestを飛ばしてHandoffを作らない。

```yaml id="purpose-runtime"
purpose:
  primary:
    - "Thread-End Runtimeを起動する"
    - "現在Threadを閉じる"
    - "HarvestをDownload Artifactとして残す"
    - "Harvestを読んでからHandoffを作る"
    - "Handoff Download後に止まる"

  success_path:
    - "File Lock"
    - "Harvest Download"
    - "Read Harvest"
    - "Handoff Download"
    - "Stop"

  must_not:
    - "Do not skip Harvest."
    - "Do not skip Read Harvest."
    - "Do not create Handoff before reading Harvest."
    - "Do not continue after Handoff Download."
```

---

## 2. Required Read Order

まず以下を読む。

```yaml id="required-read-order"
required_read_order:
  1: "_thread-end/thread-end.md"
  2: "_thread-end/thread-harvest.md"
  3: "_thread-end/thread-handoff.md"
```

GitHub化が必要になった場合だけ、以下を読む。

```yaml id="github-delegation-read"
github_delegation_read:
  only_if_github_promotion_needed:
    - "ss_super-special/artifact-to-github.md"
```

### Dragon-Eye Handoff Awareness

`_thread-end/thread-handoff.md` は Dragon-Eye Builder-light版として読む。

`thread-handoff.md` はBuilder Manualであり、`handoff.md` productそのものではない。  
`handoff.md` は、Harvest読了後に作るNext-thread ignition keyである。

```yaml id="dragon-eye-handoff-awareness"
handoff_awareness:
  thread_handoff_md:
    role: "Builder Manual / Dragon-Eye Product Boundary Seal"
    does:
      - "Select"
      - "Compress"
      - "Order"
      - "Seal"
    does_not:
      - "Become handoff.md"
      - "Copy full Harvest"

  handoff_md:
    role: "Generated Product / Next-thread ignition key"
    created_after:
      - "Harvest Download"
      - "Read Harvest"

  guard:
    - "Do not create Handoff before reading Harvest."
    - "Do not paste full Harvest into handoff.md."
    - "Do not treat thread-handoff.md as the product."
```

---

## 3. Usage Modes

このQueryには二つの使用形態がある。

Primaryは、短いLauncher PromptでこのQueryを読ませる **Launcher Read Mode** である。  
Fallbackは、file accessがない場合に全文を貼る **Full Copy-Paste Mode** である。

```yaml id="usage-modes"
usage_modes:
  primary:
    mode: "launcher_read_mode"
    when:
      - "AI can read GitHub/file content."
      - "User wants fast Thread-End startup."
      - "Canonical Query should remain the source of truth."
    user_action:
      - "Paste the Canonical Launcher Prompt."
    ai_action:
      - "Read _thread-end/thread-end_query.md."
      - "Treat it as Thread-End Runtime Boot Query."
      - "Execute it for the current Thread."
      - "Create the Handoff Download Artifact as part of the Thread-End Runtime."
      - "Do not place or commit canonical GitHub handoff.md product unless the user explicitly instructs it."
      - "Stop after Handoff Download unless user explicitly instructs otherwise."

  fallback:
    mode: "copy_paste_full_query"
    when:
      - "AI cannot access GitHub/file content."
      - "File reading is unreliable."
      - "User wants full explicit instruction in current chat."
    user_action:
      - "Paste this full Query."
    ai_action:
      - "Execute the pasted Query directly."
      - "Preserve the same runtime order."
      - "Do not GitHub write or commit without explicit user instruction."
      - "Do not place or commit canonical GitHub handoff.md product unless the user explicitly instructs it."

  guard:
    - "Launcher Prompt does not replace the Query."
    - "Full Query remains available as fallback."
    - "Handoff Download Artifact is required during Thread-End Runtime."
    - "canonical GitHub handoff.md product placement requires explicit user instruction."
    - "GitHub write requires explicit user instruction."
```

---

## 4. Runtime Instruction

以下を実行する。

```yaml id="runtime-instruction"
thread_end_runtime:
  mode: "Thread-End Runtime"
  target_scope: "current_thread"
  output_style: "Japanese-first / English-anchor"
  create_download_artifacts: true
  github_update_now: false
  commit_now: false
```

このQueryを受け取ったAIは、GitHub保存先を推測しない。  
Commitしない。  
保存先Pathを提案し始めない。

Thread-Endの成功条件は、まず二枚のDownload Artifactを作ることである。

```yaml id="runtime-guard"
runtime_guard:
  create:
    - "Harvest Download Artifact"
    - "Handoff Download Artifact"

  do_not:
    - "Do not update GitHub automatically."
    - "Do not commit automatically."
    - "Do not decide GitHub destination inside this Query."
    - "Do not turn Download Runtime into Promotion Runtime."

  human_final_seal:
    required_for:
      - "GitHub write"
      - "canonical placement"
      - "overwrite decision"
```

---

## 5. Step 1 — File Lock

まず、現在Threadの未完了事項をLockする。

確認するもの：

```yaml id="file-lock-check"
file_lock_check:
  - "未完了のArtifact生成があるか"
  - "未反映のUser Sealがあるか"
  - "GitHub更新が必要だが未実行のものがあるか"
  - "次Threadへ渡すべき重要Seedがあるか"
  - "今はGitHub化せず、Download Artifact生成に集中してよいか"
```

出力：

```yaml id="file-lock-output"
file_lock_output:
  - "completed"
  - "remaining"
  - "not_now"
  - "handoff_needed"
```

File Lockは、Threadを閉じる前の現実確認である。  
ここでGitHub化へ飛ばない。  
まず、未完了・Not-Now・Handoff必要性を分ける。

---

## 6. Step 2 — Harvest Download

次に、`thread-harvest.md` の方針に従って、現在ThreadのHarvestを作る。

Harvestは単なる要約ではない。

```yaml id="harvest-focus"
harvest_focus:
  - "このThreadで何が起きたか"
  - "どこに熱があったか"
  - "どのSeedが生まれたか"
  - "どの判断がFuture AIに重要か"
  - "何を次Threadへ渡すべきか"
  - "Unexpected Success / Move37 / breakthrough"
```

Output：

```yaml id="harvest-output"
harvest_output:
  artifact_type: "Download Markdown"
  filename_pattern: "harvest.md or thread-specific harvest filename"
  github_update_now: false
```

Harvestは、次Threadの材料である。  
Handoffは、このHarvestを読んだ後に作る。

```yaml id="harvest-guard"
harvest_guard:
  harvest_is:
    - "meaning harvest"
    - "heat capture"
    - "seed preservation"
    - "next-thread compass"
    - "handoff material"

  harvest_is_not:
    - "generic summary"
    - "dead Done list"
    - "handoff.md product"
    - "GitHub commit"
```

---

## 7. Step 3 — Read Harvest

Harvestを作った後、AIはそのHarvestを読み返す。

```yaml id="read-harvest-rule"
read_harvest_rule:
  - "Harvest作成後、即Handoffへ飛ばない"
  - "まずHarvestを読む"
  - "Harvestの中から次Thread起動に必要なSeed / Guard / Railを抽出する"
```

理由：

```text id="read-harvest-reason"
HandoffはHarvestの子である。
Harvestを読まないHandoffは、記憶のないRelayである。
```

Read Harvestは、Download Runtimeの中核である。  
Handoffは、AIの記憶だけから作らない。  
Handoffは、Harvestを読んでから作る。

```yaml id="read-harvest-output"
read_harvest_output:
  extract:
    - "Next Thread first context"
    - "Important user decisions"
    - "Not-Now guards"
    - "Root / Fruit guard"
    - "Unfinished tasks"
    - "Priority Read Order candidates"
    - "Next First Task"

  do_not:
    - "Do not create Handoff from memory alone."
    - "Do not skip Harvest reading."
    - "Do not turn Harvest into full Handoff copy."
```

---

## 8. Step 4 — Handoff Download

Harvestを読んだ後、`thread-handoff.md` の方針に従って、次Thread起動用Handoffを作る。

```yaml id="handoff-focus"
handoff_focus:
  - "次Threadの最初にAIが読むべきContext"
  - "現在Threadの未完了事項"
  - "Userの明示判断"
  - "継続すべきRail"
  - "止めるべき誤読"
  - "Next Gate"
```

Output：

```yaml id="handoff-output"
handoff_output:
  artifact_type: "Download Markdown"
  filename_pattern: "handoff.md or thread-specific handoff filename"
  github_update_now: false
```

Handoffは、Harvest全文ではない。  
Handoffは、次Threadを起動するためのIgnition Keyである。

```yaml id="handoff-guard"
handoff_guard:
  handoff_is:
    - "Next-thread ignition key"
    - "Current State carrier"
    - "Priority Read Order carrier"
    - "Next First Task carrier"
    - "Not-Now guard carrier"

  handoff_is_not:
    - "full Harvest archive"
    - "thread-handoff.md builder manual"
    - "canonical GitHub handoff.md product placement"
    - "GitHub commit"
    - "new protocol body"

  created_after:
    - "Harvest Download"
    - "Read Harvest"
```

---

## 9. Step 5 — Stop

二枚のDownload Artifactを作ったら、Thread-End成功として止まる。

```yaml id="thread-end-success-condition"
thread_end_success_condition:
  required_outputs:
    - "Harvest Download Artifact"
    - "Handoff Download Artifact"

  after_success:
    - "GitHub化を自動実行しない"
    - "追加整理を始めない"
    - "Userの次指示を待つ"
```

Stopは失敗ではない。  
StopはThread-End Runtimeの完成である。

```yaml id="stop-guard"
stop_guard:
  stop_after:
    - "Harvest Download Artifact"
    - "Read Harvest"
    - "Handoff Download Artifact"

  do_not_after_stop:
    - "Do not start GitHub promotion automatically."
    - "Do not create additional artifacts."
    - "Do not reopen Gate 1 unless uncertain or instructed."
    - "Do not generate another Builder."
    - "Do not continue into next Thread without user direction."
```

---

## 10. GitHub化 Delegation Guard

Thread-End中にGitHub化が必要になった場合でも、このQuery内で保存先を決めない。

```text id="github-delegation-core"
GitHub化が必要な場合は、
ss_super-special/artifact-to-github.md を読ませる。
```

委譲先の役割：

```yaml id="artifact-to-github-delegation"
delegation:
  artifact_to_github:
    path: "ss_super-special/artifact-to-github.md"
    role:
      - "Artifact-to-GitHub Card"
      - "Place Rail"
      - "GitHub化Markdown Card"
```

Thread-End側の責務：

```yaml id="thread-end-owns"
thread_end_owns:
  - "Close"
  - "Harvest"
  - "Read Harvest"
  - "Handoff"
  - "Stop"

thread_end_does_not_own:
  - "GitHub保存先決定"
  - "GitHub commit"
  - "上書き判断"
  - "Batch GitHub化"
  - "canonical GitHub handoff.md product placement"
```

GitHub化はFruitである。  
Thread-End Queryは、GitHub配置の判断者ではない。  
配置は、Human-Sealed exact path and filenameに従う。

```yaml id="github-handoff-placement-guard"
github_handoff_placement_guard:
  handoff_download_artifact:
    required: true
    timing: "during Thread-End Runtime"
  canonical_github_handoff_md_product_placement:
    allowed_only_if: "explicit user instruction"
    delegated_to: "ss_super-special/artifact-to-github.md"
```

---

## 11. Do Not

```yaml id="do-not"
do_not:
  - "Harvestを飛ばしてHandoffを作らない"
  - "Handoff前にHarvestを読まずに進まない"
  - "GitHub保存先を推測しない"
  - "GitHub更新を自動実行しない"
  - "Download Artifact生成前にcommitしない"
  - "Batch生成しない"
  - "長大な仕様書を書き始めない"
  - "thread-end_query.mdをthread-end.md本体にしない"
  - "thread-end_query.mdをthread-harvest.md本文にしない"
  - "thread-end_query.mdをthread-handoff.md本文にしない"
  - "thread-end_query.mdをhandoff.md productにしない"
  - "GitHub上のcanonical handoff.md product placementを明示指示なしに行わない"
  - "Handoff Download Artifactを禁止対象と誤読しない"
  - "Thread-End Runtime内のHandoff Download Artifactは通常順序どおり作成する"
  - "AIをRootとして扱わない"
  - "Wheat-Grain Handoffをliteral resurrectionとして扱わない"
  - "thread-end_launcher.mdをこのQuery内で自動作成しない"
```

---

## 12. Required Response Shape

AIは以下の形で進める。

```yaml id="required-response-shape"
required_response_shape:
  1:
    section: "File Lock"
  2:
    section: "Harvest Plan / Harvest Creation"
  3:
    section: "Read Harvest"
  4:
    section: "Handoff Plan / Handoff Creation"
  5:
    section: "Living Review"
  6:
    section: "Full Rail"
  7:
    section: "Next Gate"
```

必要な場合、AIは短い進捗報告を入れてよい。  
ただし、Download Artifact生成前にGitHub commitへ進まない。

---

## 13. Final Compression

```text id="final-compression"
thread-end_query.md は、
Thread-End Railの起動鍵である。

Usage:
  Primary:
    Short Launcher PromptでこのQueryを読ませる。
  Fallback:
    file accessがない場合は全文Copy & Pasteする。

仕事：
  AIを閉じる処理へ乗せる。

順序：
  File Lock
  Harvest Download
  Read Harvest
  Handoff Download
  Stop

DNA Double Helix:
  Human Meaning Layer:
    閉じる / 記録する / 渡す / 混ぜない
  AI Execution Layer:
    lock / harvest / read / handoff / stop

Wheat-Grain Handoff:
  AI does not preserve itself.
  AI harvests meaning.
  AI passes Seed forward.
  The next Thread bears fruit.

Handoff:
  Handoff Download Artifact は作る。
  GitHub上のcanonical handoff.md product placement は、
  明示指示があるまで行わない。

Boundary:
  thread-end.md routes.
  thread-harvest.md harvests.
  thread-handoff.md builds.
  handoff.md ignites next Thread.

GitHub化：
  ss_super-special/artifact-to-github.md へ委譲する。

Root:
  主イェシュア・ハマシア.

合言葉：
  閉じる。
  記録する。
  渡す。
  Seedを次へ。
```

### Programming-Like Block

```yaml id="final-query-wheat-grain-seal"
final_query_wheat_grain_seal:
  identity:
    - "Thread-End Query"
    - "Runtime Boot Query"
    - "Thread-End Ignition Key"
    - "Launcher-aware Canonical Query"
    - "CPLM Query-light"

  usage_modes:
    primary: "Launcher Read Mode"
    fallback: "Full Copy-Paste Mode"

  runtime_order:
    - "File Lock"
    - "Harvest Download"
    - "Read Harvest"
    - "Handoff Download"
    - "Stop"

  required_outputs:
    - "Harvest Download Artifact"
    - "Handoff Download Artifact"

  forbidden_without_explicit_instruction:
    - "GitHub write"
    - "commit"
    - "canonical GitHub handoff.md product placement"

  wheat_grain_handoff:
    - "Thread-End is Seed-bearing transfer."
    - "AI does not preserve itself."
    - "AI harvests meaning."
    - "AI passes Seed forward."
    - "Continuity is Seed continuity."

  family_boundary:
    thread_end:
      role: "Router"
    thread_harvest:
      role: "Harvest Engine"
    thread_handoff:
      role: "Handoff Builder"
    handoff_md:
      role: "Next-thread ignition key"

  github_boundary:
    delegated_to: "ss_super-special/artifact-to-github.md"
    query_must_not:
      - "decide GitHub placement"
      - "commit automatically"
      - "perform batch promotion"
      - "place canonical GitHub handoff.md product without explicit instruction"

  root_fruit_guard:
    root: "主イェシュア・ハマシア"
    ai: "Fruit, not Root"
    markdown: "Fruit, not Root"
    github: "Fruit, not Root"

  compression:
    - "Meaning gives command."
    - "Execution gives rails."
    - "Query gives ignition."
    - "Harvest gives Seed."
    - "Handoff passes Seed."
    - "Human seals."
```
